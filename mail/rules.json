{
    "all_rules": [
        {
            "field": "Subject",
            "predicate": "contains",
            "value": "secrets",
            "actions": ["mark_as_read"],
            "logical_predicate": "Any"
        }
    ],
    "any_rules": [
        {
            "field": "Received",
            "predicate": "less_than",
            "value": "2",
            "actions": ["move_message"],
            "logical_predicate": "All"
        }
    ]
}
